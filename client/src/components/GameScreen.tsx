import { useMemo, useState } from 'react';
import { Socket } from 'socket.io-client';
import MobileGameLayout from './MobileGameLayout';
import MoveHistory from './MoveHistory';
import CapturedPieces from './CapturedPieces';
import ChatBox from './ChatBox';
import ThemeCustomizer from './ThemeCustomizer';
import HelpModal from './HelpModal';

export default function GameScreen({ socket, room, role, playerId, onLeave }: { socket: Socket | null; room: any; role: string | null; playerId: string; onLeave: () => void }) {
  const [showTheme, setShowTheme] = useState(false); const [showHelp, setShowHelp] = useState(false);
  const isPlayer = role === 'red' || role === 'black'; const seat = role === 'red' ? room.red : role === 'black' ? room.black : null;
  const theme = useMemo(() => room.settings.theme || {}, [room.settings.theme]);
  const share = `${location.origin}${location.pathname}?room=${room.id}`;
  const statusText = room.game.status === 'waiting' ? 'Đang chờ sẵn sàng' : room.game.status === 'playing' ? `Lượt ${room.game.turn === 'red' ? 'Đỏ' : 'Đen'}` : room.game.winner ? `${room.game.winner === 'red' ? 'Đỏ' : 'Đen'} thắng` : 'Hòa';
  const copy = () => navigator.clipboard?.writeText(share);
  const updateTheme = (t: any) => socket?.emit('room:updateSettings', { theme: t });
  return <main className="game-screen"><header className="game-header"><div><h1>{room.name}</h1><p>Mã bàn <b>{room.id}</b> · {statusText} · Vai trò: {role === 'spectator' ? 'Quan sát' : role === 'red' ? 'Đỏ' : 'Đen'}</p></div><div className="actions"><button className="secondary" onClick={copy}>Copy link</button><button className="secondary" onClick={onLeave}>Rời bàn</button></div></header>{room.game.status === 'waiting' && <section className="card waiting"><h2>Phòng chờ</h2><p>Đỏ: {room.red?.name || 'trống'} {room.red?.ready ? '✅' : ''} · Đen: {room.black?.name || 'trống'} {room.black?.ready ? '✅' : ''}</p>{isPlayer && <button onClick={() => socket?.emit('game:ready', { ready: !seat?.ready })}>{seat?.ready ? 'Hủy sẵn sàng' : 'Sẵn sàng'}</button>}<p className="hint">Khi có đủ hai người chơi và cả hai bấm sẵn sàng, ván bắt đầu. Đỏ đi trước.</p></section>}<div className="game-grid"><section><MobileGameLayout room={room} role={role} socket={socket} theme={theme}/><CapturedPieces captured={room.game.captured}/><div className="actions game-actions">{isPlayer && <><button onClick={() => socket?.emit('undo:request')}>Xin hoàn cờ</button><button onClick={() => socket?.emit('draw:request')}>Xin hòa</button><button className="danger-btn" onClick={() => confirm('Bạn chắc chắn đầu hàng?') && socket?.emit('resign:confirm')}>Đầu hàng</button></>}<button className="secondary" onClick={() => setShowTheme(!showTheme)}>Tùy chỉnh</button><button className="secondary" onClick={() => setShowHelp(true)}>Hướng dẫn</button>{room.game.status === 'ended' && <button onClick={() => socket?.emit('game:newRequest')}>Tạo ván mới</button>}</div>{room.pendingUndo && isPlayer && room.pendingUndo.by !== role && <div className="request card"><b>Đối thủ muốn hoàn lại nước vừa đi.</b><button onClick={() => socket?.emit('undo:accept')}>Đồng ý</button><button className="secondary" onClick={() => socket?.emit('undo:reject')}>Từ chối</button></div>}{room.pendingDraw && isPlayer && room.pendingDraw.by !== role && <div className="request card"><b>Đối thủ xin hòa.</b><button onClick={() => socket?.emit('draw:accept')}>Đồng ý</button><button className="secondary" onClick={() => socket?.emit('draw:reject')}>Từ chối</button></div>}{showTheme && <ThemeCustomizer theme={theme} onChange={updateTheme}/>}</section><aside><MoveHistory moves={room.game.moveHistory}/><ChatBox socket={socket} room={room}/><section className="card"><h3>Người quan sát</h3><p>{room.spectators?.length || 0} người</p>{room.ownerPlayerId === playerId && <div className="actions"><button className="secondary" onClick={() => socket?.emit('room:updateSettings', { spectatorChatEnabled: !room.settings.spectatorChatEnabled })}>{room.settings.spectatorChatEnabled ? 'Tắt chat người xem' : 'Bật chat người xem'}</button><button className="secondary" onClick={() => socket?.emit('room:updateSettings', { allowSpectators: !room.settings.allowSpectators })}>{room.settings.allowSpectators ? 'Tắt quan sát' : 'Bật quan sát'}</button></div>}</section></aside></div>{showHelp && <HelpModal onClose={() => setShowHelp(false)}/>}</main>;
}
